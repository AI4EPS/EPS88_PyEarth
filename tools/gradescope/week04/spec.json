{
  "week": 4,
  "notebook_stem": "04_where_and_why",
  "other_stems": [
    "01_birthquake",
    "02_liquid_water",
    "03_two_peaks",
    "05_clustered_or_random",
    "06_age_of_the_universe",
    "07_how_often_tambora",
    "08_wrong_or_unlucky",
    "09_rising_faster",
    "10_earthquake_or_explosion",
    "11_where_magma",
    "12_hidden_fault",
    "13_machine_hears",
    "14_project"
  ],
  "homework_heading": "## Homework",
  "parts": [
    {
      "name": "Part 1 — magnitudes on a log axis",
      "marker": "✓ magnitudes on a log axis",
      "checks": [
        "assert counts.sum() == len(quakes), \"every earthquake should land in a bin — check your edges\""
      ],
      "check_points": 30,
      "code_points": 5
    },
    {
      "name": "Part 2 — the slab",
      "marker": "✓ the slab",
      "checks": [
        "assert len(arc) > 0, \"no earthquakes in that box — check the four numbers\"",
        "assert section.get_offsets()[:, 1].max() <= 0, \\",
        "assert 1 < abs(deep_lon - shallow_lon) < 15, \\"
      ],
      "check_points": 30,
      "code_points": 5
    }
  ],
  "released": {
    "w04-q06-answer": "# ← your answer here\n\n\nassert counts.sum() == len(quakes), \"every earthquake should land in a bin — check your edges\"\nprint(\"✓ magnitudes on a log axis — the smallest bin holds\", counts[0],\n      \"earthquakes and the next one\", counts[1])",
    "w04-q07-answer": "# ← your answer here\n\n\nassert len(arc) > 0, \"no earthquakes in that box — check the four numbers\"\nassert section.get_offsets()[:, 1].max() <= 0, \\\n    \"the deepest events are drawn at the TOP — plot -arc[\\\"depth\\\"], so that down the page is down\"\nassert 1 < abs(deep_lon - shallow_lon) < 15, \\\n    \"the two medians should be a few degrees apart; a gap of nearly nothing usually means both \" \\\n    \"were taken over the same depth class\"\nif deep_lon > shallow_lon:\n    side = \"east\"\nelse:\n    side = \"west\"\nprint(\"✓ the slab —\", len(arc), \"earthquakes; the deep ones sit\",\n      round(abs(deep_lon - shallow_lon), 1), \"degrees\", side, \"of the shallow ones\")"
  },
  "hygiene": {
    "points": 5
  },
  "manual": {
    "parts": 1,
    "points_each": 25,
    "points": 25,
    "note": "Part 3 of 3 is answered in prose and is marked by hand. Add a 25-point manual rubric item in Gradescope; the autograder is out of 75."
  },
  "autograder_max": 75
}
